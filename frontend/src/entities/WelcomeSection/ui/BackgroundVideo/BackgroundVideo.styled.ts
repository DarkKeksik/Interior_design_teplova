import styled from "styled-components"

export const Wrap = styled.div`
  position: absolute;
  top: 0;
  left: 0;
  filter: blur(0);
  z-index: 2;
  height: 100%;
  width: 100%;
  overflow: hidden;
`
